import React from 'react';
import {AppRegistry} from 'react-native';
import App from './src';

/*
* TODO AwesomeProject 必须是这个名称，不知道哪里有关联。。。
* */
AppRegistry.registerComponent('AwesomeProject', () => App);